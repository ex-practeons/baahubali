server:
  port: 8080

spring:
  application:
    name: api-gateway
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: iam-service-auth
              uri: ${IAM_SERVICE_URL:http://localhost:8080}
              predicates:
                - Path=/api/auth/**
            - id: iam-service-users
              uri: ${IAM_SERVICE_URL:http://localhost:8080}
              predicates:
                - Path=/api/users/**
            - id: test-service
              uri: ${TEST_SERVICE_URL:http://localhost:8080}
              predicates:
                - Path=/api/tests/**

logging:
  level:
    org.springframework.cloud.gateway: TRACE

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    urls:
      - name: IAM Service
        url: /api/auth/v3/api-docs
      - name: Exam Engine
        url: /api/exams/v3/api-docs

gateway:
  security:
    public-paths:
      - /api/auth/login
      - /api/auth/register
      - /swagger-ui.html
      - /swagger-ui/**
      - /v3/api-docs/**
      - /api/auth/v3/api-docs
      - /api/exams/v3/api-docs
      - /webjars/**
    jwt:
      secret: ${JWT_SECRET:MTIzNDU2Nzg5MDEyMzQ1Njc4OTAxMjM0NTY3ODkwMTI}
      cookie-name: ACCESS_TOKEN
      user-id-claim: sub
      role-claim: role
      issuer: ${JWT_ISSUER:}
      clock-skew-seconds: 30