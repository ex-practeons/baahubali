spring:
  application:
    name: api-gateway
  # Gateway routes go here, e.g.:
  # cloud:
  #   gateway:
  #     server:
  #       webflux:
  #         routes:
  #           - id: payment-service
  #             uri: http://localhost:8081
  #             predicates:
  #               - Path=/api/payments/**

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
